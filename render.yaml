services:
- type: web
name: casino-dice-bot
env: python
buildCommand: pip install -r requirements.txt
startCommand: gunicorn bot:flask_app --bind 0.0.0.0:$PORT --workers 1 --threads 2 --timeout 120
plan: free
envVars:
- key: TOKEN
sync: false
- key: ADMIN_ID
sync: false
- key: WEB_URL
sync: false
